StockSync Pro v18.11.5 CLEAN

Deploy all files/folders in this directory.

v18.11.5 bill scan fixes:
- stronger PDF row extraction and hybrid OCR fallback
- handles Qty→UOM and UOM→Qty table layouts, wrapped product names, HSN/code noise, model numbers and repeated lines
- smarter automatic Item Master mapping using cleaned product-name variants
- learned OCR mappings also reuse safe cleaned variants
- local PDF scan supports up to 30 pages; backend Cloud Vision scan now checks successive 5-page windows up to 30 pages

IMPORTANT: functions/index.js changed in this release. If backend document OCR is enabled, redeploy the Firebase Functions so multi-page backend scanning uses the new logic. Firestore and Storage rules are unchanged from v18.11.4.

After deploy: hard refresh (Ctrl+Shift+R) or fully close/reopen the installed PWA.
